// js general
import { setupScrollAnimations } from './animationCards.js';
import { setupCardEvents } from './eventsCards.js';

document.addEventListener('DOMContentLoaded', () => {
  setupScrollAnimations();
  setupCardEvents();
});

console.log("Hello")

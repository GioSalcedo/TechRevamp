<!DOCTYPE html>
<html lang="es">
  <head>
    <%- include('partials/header');%>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
      crossorigin="anonymous"
    />
  </head>
  <body>
    <header>
      <%- include('partials/navbar');%> 
    </header>
    <div class="header-recomendation">
      <h2>Hello, ‘@Username’</h2>
      <p>Tenemos algunas recomendaciones para usted</p>
    </div>
    <div class="header-botton-recomendation">
      <p>Sólo para ti</p>
      <a href="#">Ver Todo</a>
    </div>
    <div class="card-container-recommendation">
      <% Object.keys(productos.productos).forEach(function(key) { %>
        <% const producto = productos.productos[key]; %>
        <% if (producto && producto.nombre) { %>
          <%-//  include('partials/card-recommendation', {producto: producto}); %>
        <% } %>
      <% }); %>
    </div>
    <h3>Ofertas</h3>
    <section class="galery-products">
      <div class="container-products">
        <% Object.keys(productos.productos).forEach(function(key) { %>
          <% const producto = productos.productos[key]; %>
          <% if (producto && producto.nombre) { %>
            <%- include('partials/card-product', {producto: producto}); %>
          <% } %>
        <% }); %>
      </div>
    </section>

    <!-- <%- // include('partials/arrow-button');%>  -->
    <!-- <%- // include('partials/categories');%> -->
    <%- include('partials/footer');%> 
    <%- include('partials/scripts');%>
  </body>
</html>
